{
  "name": "eims-functions",
  "description": "EIMS Cloud Functions: claim scoring, settlement, statutory filing",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "check": "node ../check-rules.js",
    "serve": "npx -y firebase-tools@latest emulators:start --only functions,firestore",
    "deploy": "npx -y firebase-tools@latest deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "private": true
}
